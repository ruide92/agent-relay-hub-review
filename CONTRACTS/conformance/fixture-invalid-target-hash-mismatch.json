{
  "fixture_id": "invalid-target-hash-mismatch",
  "target_schema": "CONTRACTS/policy-bundle.schema.json",
  "expected": "invalid",
  "expected_violation": {
    "layer": "business",
    "rule": "sidecar signature-manifest target_hash does not match content JCS canonical bytes SHA-256"
  },
  "note": "结构示例，非密码学有效证据; business-layer hash verification",
  "instance": {
    "schema_version": "1.0.0",
    "bundle_id": "pb-002",
    "policy_version": "1.0.0",
    "authorization_tiers": [
      {
        "tier": "A0",
        "allowed_scopes": [
          "workflow:read"
        ]
      }
    ],
    "scope_registry": [
      "workflow:read"
    ],
    "default_decision": "deny"
  }
}
